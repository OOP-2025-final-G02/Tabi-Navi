# バックエンド実装ガイド - 詳細手順

完了日: 2025年12月27日

---

## ✅ 実装完了項目

### フェーズ1: 基礎設定 ✅ 完了

#### 1-1: SQLiteデータベース設定
- ✅ `backend/app/database/db.py` - SQLAlchemy設定
- ✅ `backend/app/database/__init__.py` - パッケージ初期化
- 機能:
  - SQLiteへの自動接続
  - セッション管理
  - DB初期化関数 `init_db()`
  - 古いプラン自動削除 `cleanup_old_plans(days=365)`

#### 1-2: DBモデル定義
- ✅ `backend/app/models/db_models.py` - SQLAlchemy ORM
- 定義済みテーブル:
  - **TravelPlanDB**: プラン全体保存
  - **TimelineItemHistory**: 編集履歴記録

#### 1-3: 環境変数設定
- ✅ `backend/.env.example` 更新
- ✅ `backend/config.py` 拡張
- 追加環境変数:
  - `GEMINI_API_KEY` (必須)
  - `DATABASE_URL` (デフォルト: SQLite)
  - `AI_REQUEST_TIMEOUT` (デフォルト: 30秒)
  - `PLAN_AUTO_DELETE_DAYS` (デフォルト: 365日)

---

### フェーズ2: Gemini API統合 ✅ 完了

#### 2-1: Gemini API通信用サービス
- ✅ `backend/app/services/gemini_service.py` - API通信
- メソッド:
  - `call_gemini(prompt, temperature, max_tokens)` - API呼び出し
  - `save_request_response(plan_id, request_data, response_data)` - JSON記録
- 機能:
  - JSONレスポンス自動パース
  - エラーハンドリング
  - リクエスト・レスポンスのJSONファイル記録

#### 2-2: プロンプト設計
- ✅ `backend/app/services/prompts/travel_plan_prompt.py`
- 機能:
  - 旅行条件をJSON形式でプロンプト化
  - Gemini APIが返すJSON形式を指定

#### 2-3: PlanGeneratorService更新
- ✅ `backend/app/services/plan_generator.py`
- 準備完了（次のステップで実装）

---

### フェーズ3: DB保存・取得 ✅ 完了

#### 3-1: プラン保存エンドポイント（内部使用）
- ✅ `backend/app/services/plan_storage_service.py`
- メソッド:
  - `save_plan(plan, db)` - プラン保存
  - `get_plan(plan_id, db)` - プラン取得
  - `update_plan(plan_id, updated_plan, db)` - プラン更新
  - `delete_plan(plan_id, db)` - プラン削除
  - `get_all_plans(db, limit, offset)` - プラン一覧取得

#### 3-2: DBエンドポイント作成
- ✅ `backend/app/routes/storage.py`
- エンドポイント:
  - `GET /api/storage/plans/history` - プラン一覧
  - `GET /api/storage/plans/{plan_id}` - プラン取得
  - `DELETE /api/storage/plans/{plan_id}` - プラン削除
  - `GET /api/storage/plans/{plan_id}/edit-history` - 編集履歴取得

---

### フェーズ4: プラン編集機能 ✅ 完了

#### 4-1: 編集ロジック設計
- ✅ `backend/app/routes/plan.py` 拡張
- 編集可能な要素:
  - 時刻: "09:00" → "10:00"
  - スポット名: "沖縄の観光スポット1" → ユーザー入力
  - カテゴリ: "観光" → "グルメ"
  - 滞在時間: 120分 → ユーザー入力値
  - 費用: 500円 → ユーザー入力値
  - 移動手段メソッド: "電車" → "バス"
  - 説明/詳細: テキスト編集

#### 4-2: 編集エンドポイント実装
- ✅ `backend/app/routes/plan.py` - 以下のエンドポイント実装:
  - `POST /api/plans/{plan_id}/items/{day}/{item_index}/edit` - アイテム編集
  - `POST /api/plans/{plan_id}/items/{day}/{item_index}/delete` - アイテム削除
  - `POST /api/plans/{plan_id}/items/{day}/add` - アイテム追加

#### 4-3: 編集履歴管理
- ✅ `backend/app/services/history_service.py`
- メソッド:
  - `record_edit(...)` - 編集を履歴に記録
  - `get_history(plan_id, db)` - 履歴を取得
  - `get_history_count(plan_id, db)` - 編集回数カウント
  - `clear_history(plan_id, db)` - 履歴クリア（オプション）

#### 4-4: バリデーション
- ✅ `backend/app/utils/validators.py` - EditValidator クラス
- バリデーション対象:
  - 時刻: HH:MM形式（00:00～23:59）
  - スポット名: 1～200文字
  - カテゴリ: 事前定義されたカテゴリのみ
  - 滞在時間: 1～1440分
  - 費用: 0～1000000円
  - 日付: 1～総日数
  - アイテムインデックス: 有効範囲内
  - 移動手段: 事前定義されたもののみ
  - 編集フィールド: 有効フィールドのみ

---

### フェーズ5: 統合とテスト ✅ 部分完了

#### 5-1: main.py更新
- ✅ `backend/app/main.py` 完全更新
- 追加機能:
  - `lifespan` コンテキストマネージャー
  - アプリケーション起動時にDB初期化
  - 古いプラン自動削除機能
  - `storage` ルーター登録

#### 5-2: エラーハンドリング
- ✅ `backend/app/utils/exceptions.py` - カスタム例外
- 定義済み例外:
  - `PlanNotFoundError`
  - `InvalidEditError`
  - `ValidationError`
  - `DatabaseError`
  - `GeminiAPIError`

#### 5-3: ユニットテスト
- ⏳ 未実装（次のステップ）

#### 5-4: ドキュメント更新
- ⏳ 部分実装（このファイル）

---

### その他
- ✅ `backend/requirements.txt` 更新
- ✅ `backend/.env.example` 更新
- ✅ `backend/data/gemini_logs/` ディレクトリ作成
- ✅ `.gitignore` 更新

---

## 🚀 次のステップ

### ステップ1: 環境設定と依存関係インストール

```bash
# 1. backendディレクトリに移動
cd /Users/k24101kk/TabiNavi/taviNavi/backend

# 2. 依存関係をインストール
pip install -r requirements.txt

# 3. .envファイルを作成（.env.exampleをコピー）
cp .env.example .env

# 4. .envを編集してGemini APIキーを設定
# GEMINI_API_KEY=your_actual_gemini_api_key_here
```

### ステップ2: PlanGeneratorServiceの実装

現在の `plan_generator.py` はダミーデータを返しています。Gemini APIを呼び出すように更新する必要があります：

```python
async def generate_plan(self, travel_input: TravelInput) -> TravelPlan:
    # 1. GeminiServiceを初期化
    # 2. プロンプトを生成
    # 3. Gemini APIを呼び出し
    # 4. レスポンスをTravelPlanに変換
    # 5. JSONログを保存
```

### ステップ3: フロントエンドとの連携

フロントエンド担当者に以下を通知：

1. **IndexedDB実装**: ブラウザローカルストレージにプラン保存
2. **エンドポイント一覧**:
   - `POST /api/plans/generate` - プラン生成
   - `POST /api/plans/{plan_id}/items/{day}/{item_index}/edit` - 編集
   - `POST /api/plans/{plan_id}/items/{day}/{item_index}/delete` - 削除
   - `POST /api/plans/{plan_id}/items/{day}/add` - 追加
   - `GET /api/storage/plans/history` - プラン一覧取得
   - `GET /api/storage/plans/{plan_id}` - プラン詳細取得
   - `GET /api/storage/plans/{plan_id}/edit-history` - 編集履歴取得

### ステップ4: テスト

```bash
# 単体テストの実行
pytest backend/tests/

# サーバー起動テスト
python app/main.py
```

---

## 📊 エンドポイント一覧

### プラン生成
```
POST /api/plans/generate
Content-Type: application/json

Request:
{
  "origin": "東京",
  "destination": "京都",
  "start_date": "2025-12-26",
  "end_date": "2025-12-28",
  "budget": 50000,
  "travelers": 2,
  "interests": ["グルメ", "歴史"],
  "must_visit": "清水寺"
}

Response:
{
  "plan_id": "uuid-string",
  "input_data": {...},
  "schedules": [...],
  "total_cost": 50000,
  "total_duration": 1440,
  "created_at": "2025-12-27T12:00:00"
}
```

### プランアイテム編集
```
POST /api/plans/{plan_id}/items/1/0/edit
Content-Type: application/json

Request:
{
  "field": "time",
  "new_value": "10:00"
}

Response:
{
  "success": true,
  "data": {...},
  "message": "編集が完了しました"
}
```

### プランアイテム追加
```
POST /api/plans/{plan_id}/items/1/add
Content-Type: application/json

Request:
{
  "time": "14:00",
  "type": "spot",
  "name": "新しいレストラン",
  "category": "グルメ",
  "duration": 90,
  "cost": 2000,
  "description": "地元の名物料理"
}

Response:
{
  "success": true,
  "data": {...},
  "message": "アイテムを追加しました"
}
```

### プラン一覧取得
```
GET /api/storage/plans/history?limit=10&offset=0

Response:
{
  "success": true,
  "data": [
    {
      "plan_id": "uuid",
      "destination": "京都",
      "start_date": "2025-12-26",
      "end_date": "2025-12-28",
      "budget": 50000,
      "created_at": "2025-12-27T12:00:00",
      "updated_at": "2025-12-27T12:00:00"
    }
  ],
  "count": 1
}
```

---

## 🔑 重要な仕様

### 1. IndexedDB連携（フロントエンド向け）
- **目的**: ユーザーログイン不要で端末内にプラン履歴を保存
- **動作**: バックエンドエンドポイントとIndexedDBの同期
- **利点**: Webキャッシュ的に機能、プライベート

### 2. プラン自動削除
- **ルール**: 作成から365日以上経過したプランは自動削除
- **実行タイミング**: アプリケーション起動時
- **設定**: 環境変数 `PLAN_AUTO_DELETE_DAYS`

### 3. 編集履歴
- **保存期間**: 無制限
- **内容**: 変更前後のデータ、操作タイプ、時刻
- **復元機能**: なし（保存のみ）

### 4. 総費用の自動再計算
- 編集・追加・削除時に自動計算
- すべてのアイテムの合計費用

---

## 🐛 トラブルシューティング

### Gemini APIキーエラー
```
❌ GEMINI_API_KEY環境変数が設定されていません
```
→ `.env` ファイルに有効なAPIキーを設定してください

### DB接続エラー
```
❌ データベース操作に失敗しました
```
→ `backend/data/` ディレクトリが存在するか確認

### JSONパースエラー
```
❌ JSON パースエラー
```
→ Gemini APIのレスポンス形式を確認

---

## 📝 変更履歴

| 日付 | 項目 | 状態 |
|------|------|------|
| 2025-12-27 | DB設定・モデル定義 | ✅ 完了 |
| 2025-12-27 | Gemini API統合基盤 | ✅ 完了 |
| 2025-12-27 | プラン保存・取得 | ✅ 完了 |
| 2025-12-27 | 編集機能エンドポイント | ✅ 完了 |
| 2025-12-27 | バリデーション | ✅ 完了 |
| - | PlanGeneratorService実装 | ⏳ 次のステップ |
| - | テスト実装 | ⏳ 次のステップ |

---

**作成者**: バックエンド実装チーム  
**最終更新**: 2025年12月27日
